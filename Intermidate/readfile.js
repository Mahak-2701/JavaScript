//asynchronized method/non-blocking
const fs=require('fs');
fs.readFile("./mahak.js","utf-8",function(err,data){
    if(err) throw err;
    else
    console.log(data);
})
const fs=require('fs');
fs.readFile("./mahak.js","utf-8",function(err,data){
    if(err) throw err;
    else
    console.log(data);
})
